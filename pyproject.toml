[project]
name = "fserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.0",
]

[[tool.uv.index]]
# Required URL for the index.
url = "https://pip.baidu-int.com/simple/"
default=true

